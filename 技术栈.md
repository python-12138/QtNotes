# 技术栈记录

> 本项目（记账本 PWA）使用的技术框架与依赖，含版本与用途说明。

## 项目概览

- **类型**：PWA（渐进式 Web 应用）
- **目标平台**：iOS（Safari 添加到主屏幕）/ Android（Chrome）/ 桌面浏览器
- **数据**：纯本地存储（IndexedDB），无后端、无账号系统
- **部署**：GitHub Actions 自动构建 → GitHub Pages 免费托管

## 核心技术栈

| 技术 | 版本 | 用途 |
|---|---|---|
| Vue | ^3.5.42 | 前端 UI 框架（组合式 API，`<script setup>`） |
| TypeScript | ^5.9.3 | 类型安全（记账涉及金额，减少 bug） |
| Vite | ^8.2.2 | 构建工具与开发服务器 |
| Dexie.js | ^4.4.5 | IndexedDB 封装，本地离线数据库 |
| ECharts | ^6.1.0 | 统计图表（饼图、柱状图） |
| vue-echarts | ^8.2.0 | ECharts 的 Vue 封装组件 |
| dayjs | ^1.11.23 | 日期格式化与运算 |
| vite-plugin-pwa | ^1.3.0 | PWA 支持（manifest + Service Worker） |

## 开发依赖

| 技术 | 版本 | 用途 |
|---|---|---|
| @vitejs/plugin-vue | ^6.0.8 | Vite 的 Vue 插件 |
| vue-tsc | ^3.3.11 | Vue 类型检查（替代 `tsc --noEmit`） |
| sharp | ^0.35.4 | 生成 PWA PNG 图标（仅开发期用，`npm run icons`） |

> 注：TypeScript 需固定在 5.x，因为 `vue-tsc` 尚不支持 TypeScript 7（原生版）的包结构。

## 架构说明

```
src/
├── db/            数据层：类型定义、Dexie 数据库、种子数据、预设图标/颜色/油费类型
├── store/         响应式数据 hooks（Dexie liveQuery 桥接成 Vue ref）
├── plugins/       ECharts 按需注册
├── components/    通用组件：金额键盘、账目条目、分类选择器、账本切换、弹窗等
├── pages/         页面：首页 / 记一笔 / 明细 / 统计 / 设置
├── utils/         工具：金额换算、日期、id、主题、油耗计算
└── styles/        全局样式（CSS 变量，浅色/深色主题）
```

- **数据表**：`ledgers`（账本）、`transactions`（流水，含可选加油字段）、`categories`（分类）、`accounts`（账户）
- **导航**：底部 Tab 栏 + 悬浮「＋」按钮，无路由库（状态切换）

## 关键设计决策

1. **金额以「分」（整数）存储**：避免浮点误差，展示时格式化为「元」（两位小数 + 千分位）。
2. **IndexedDB 而非 SQLite**：浏览器原生本地数据库，与 SQLite 同类，无需 WebAssembly 引入额外复杂度。
3. **多账本隔离**：每个表带 `ledgerId`，所有查询按当前账本过滤，实现账本间完全独立。
4. **加油记录复用 transactions**：用可选字段 `fuelType/liters/km` 标记油费记录，配合 `Category.isFuel`/`protected` 实现油耗计算与「系统分类不可删」。
5. **PWA 而非原生 App**：无 Mac、无 Apple 开发者账号的前提下，唯一免费可安装到 iPhone 的方案。
6. **`base: './'` 相对路径**：保证部署到 GitHub Pages 的 `/仓库名/` 子路径时无需改配置。
7. **数据迁移**：通过 JSON 导出/导入实现，格式为标准开放 JSON，未来可无缝迁入原生 App。
